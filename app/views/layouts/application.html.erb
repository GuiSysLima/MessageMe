<!DOCTYPE html>
<html>
<head>
  <title>MessageMe</title>
  <%= favicon_link_tag %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= action_cable_meta_tag %>
  
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>
  <body>
    <div class="ui small inverted menu">
      <div class="ui container">
        <%= render 'layouts/navigation' %>
      </div>
    </div>

  <div class="ui container">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </div>
  <script>
    (function() {

      var waitForTurbo = function() {

        if (typeof ActionCable === 'undefined' || typeof Turbo === 'undefined') {
          console.log("⏳ Aguardando ActionCable e Turbo carregarem...");
          setTimeout(waitForTurbo, 100);
          return;
        }

        if (!window.App) window.App = {};

        if (!window.App.cable) {
          console.log("🚀 [1/2] Action Cable Criado.");
          window.App.cable = ActionCable.createConsumer();
        }

        if (Turbo.connectStreamSource) {
          console.log("🚀 [2/2] Conectando Turbo ao Stream (SUCESSO).");
          Turbo.connectStreamSource(window.App.cable);
        } else {
          console.log("🚀 [2/2] Forçando assinatura manual.");
          window.App.cable.subscriptions.create("ActionCable::Channel");
        }
      };

      waitForTurbo();
    })();
  </script>
  </body>
</html>
